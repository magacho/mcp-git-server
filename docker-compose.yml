services:
  mcp-git-server:
    build: .
    container_name: mcp-git-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
