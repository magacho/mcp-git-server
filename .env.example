# Configuração do Repositório
REPO_URL=https://github.com/seu-usuario/seu-repo.git
REPO_BRANCH=main

# === PRIVATE REPOSITORIES ===

# GitHub Private Repositories
# Create token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos)
# GITHUB_TOKEN=ghp_your_personal_access_token_here

# Bitbucket Private Repositories
# Create app password at: https://bitbucket.org/account/settings/app-passwords/
# Required permissions: repository:read
# BITBUCKET_USERNAME=your_username
# BITBUCKET_APP_PASSWORD=your_app_password

# GitLab Private Repositories
# Create token at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: read_repository
# GITLAB_TOKEN=glpat-your_personal_access_token_here

# Generic Git Authentication (fallback)
# GIT_TOKEN=your_token
# GIT_USERNAME=your_username

# Alternative: Use SSH URL with configured SSH keys
# GitHub: REPO_URL=git@github.com:user/repo.git
# Bitbucket: REPO_URL=git@bitbucket.org:workspace/repo.git
# GitLab: REPO_URL=git@gitlab.com:user/repo.git

# Configuração de Embeddings
# Opções: 'openai', 'sentence-transformers', 'huggingface', 'auto'
# Padrão: sentence-transformers (gratuito)
EMBEDDING_PROVIDER=sentence-transformers

# Para OpenAI (opcional - pago, alta qualidade)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Para modelos locais (gratuito, boa qualidade)
# Sentence Transformers
ST_EMBEDDING_MODEL=all-MiniLM-L6-v2

# HuggingFace
HF_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Configuração de Contagem de Tokens
# Opções: 'local' (rápido), 'tiktoken' (preciso), 'auto'
# Padrão: local (rápido e gratuito)
TOKENIZER_MODE=local

# === SEGURANÇA ===
# API Key para proteger endpoints
# Se não definida, a API fica aberta (modo desenvolvimento)
# API_KEY=your-secret-api-key-here

# === LOGGING ===
# Nível de log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO